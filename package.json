{
  "name": "@vitest-preview/workspace",
  "version": "0.0.1",
  "description": "Visual Debugging Experience for Vitest 🧪🖼⚡️",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "CI=true pnpm run  -r --if-present test",
    "build": "pnpm run -r --filter './packages/**' --if-present build",
    "prepublishOnly": "pnpm run build",
    "prettier": "prettier examples packages --check",
    "prettier:fix": "pnpm run prettier --write",
    "docs:dev": "cd website && pnpm dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "prettier": "^3.3.2",
    "rimraf": "^5.0.7"
  }
}
